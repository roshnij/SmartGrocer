// import ConfigCuisine from '../index';

import expect from 'expect';
// import { shallow } from 'enzyme';
// import React from 'react';

describe('<ConfigCuisine />', () => {
	it('Expect to have unit tests specified', () => {
		expect(false).toEqual(false);
	});
});
